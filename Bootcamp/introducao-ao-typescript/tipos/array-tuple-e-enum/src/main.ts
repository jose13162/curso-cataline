// array
let items: Array<number>;
items = [1, 2, 3, 4, 5];

let array: number[];

// tuple
let elements: [number, string, boolean];
elements = [0, "0", true];

// enum
enum Colors {
  white = "#fff",
  black = "#000"
}

let white: Colors = Colors.white;
console.log(white);